tokens:
  text: .+

grammar:
  main: text

code:
  init: | # py
    import requests
    r = requests.get(main())
    r.raise_for_status()
    return r.text
  # main: raise Exception('hi')
  main: raise Exception('hi')
  # main: $ echo {num[0]}{locals()[{][0]}
  # main: $ echo {num[0]}{!}

requirements: requests
environment: test