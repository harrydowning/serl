tool: 0.0.1
name: gantt

rules:
  text: "([a-zA-Z0-9\s]*)"
  title: text
  -----: ---+
  bar: \s*=*
  id: (\d+)
  ref: "\[id\]"
  row: text \| (bar \*?) (?| ref | text | ())? \n?
  rows: "(?: row)+"

block:
  def: "[id]: text"
  gantt: |
      title
      -----
      rows

---

name: lang2
d: 2
